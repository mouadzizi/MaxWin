import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function ImagesBrowser() {
	return (
		<View>
			<Text>Images browser</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
